import BaseSearchComponent from '../../../_shared/_components/BaseSearchComponent.mdx';

### SearchBar

To create your own search bar that allows the user to search for recipes & products on the catalog, you create a class that implements `BaseSearchComponent`.

<BaseSearchComponent />