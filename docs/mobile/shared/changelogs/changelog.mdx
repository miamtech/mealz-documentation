## 1.0.0

Initial MealzUIKit documentation cut for the WebView + SSR mobile stack:

- Single artifact (`ai.mealz:ui-kit` / `MealzUIKit`)
- `SdkConfiguration` + `BasketSynchronizationManager`
- Catalog, Mealz Planner, recipe card/details/tags, no-supplier helpers
- Analytics bridge to SSR events