import Link from '@docusaurus/Link';
import Admonition from '@theme/Admonition';

export function InitIntro(props) {
  if (props.platform === "android") {
    return (
      <p>
        After installing MealzUIKit, start the library once at app launch (for example in <code>Application.onCreate</code>).
      </p>
    );
  }
  if (props.platform === "ios") {
    return (
      <p>
        After installing MealzUIKit, start the library once at app launch (for example in <code>AppDelegate</code> / <code>@main</code> app init).
        On iOS, Kotlin <code>object</code> APIs are accessed via <code>.shared</code> (for example <code>SdkConfiguration.shared</code>).
      </p>
    );
  }
  return (
    <p>
      After installing MealzUIKit, start the library once from your KMM app entry (platform <code>SdkContext</code> supplied from Android / iOS actuals).
    </p>
  );
}

<InitIntro platform={props.platform}/>

## Start the Library

export function StartLibrary(props) {
  if (props.platform === "android") {
    return (
      <pre><code className="language-kotlin">{`import ai.mealz.uikit.config.SdkConfiguration
import ai.mealz.uikit.config.SdkContext
import ai.mealz.uikit.model.Environment

SdkConfiguration.start(
    supplierKey = WEB_SUPPLIER_KEY, // base64-encoded web key
    context = SdkContext(applicationContext),
    environment = Environment.PROD // or Environment.DEV
)`}</code></pre>
    );
  }
  if (props.platform === "ios") {
    return (
      <pre><code className="language-swift">{`import MealzUIKit

SdkConfiguration.shared.start(
    supplierKey: WEB_SUPPLIER_KEY, // base64-encoded web key
    context: SdkContext(),
    environment: .prod // or .dev
)`}</code></pre>
    );
  }
  return (
    <pre><code className="language-kotlin">{`import ai.mealz.uikit.config.SdkConfiguration
import ai.mealz.uikit.config.SdkContext
import ai.mealz.uikit.model.Environment

// From commonMain — pass a platform-provided SdkContext
SdkConfiguration.start(
    supplierKey = WEB_SUPPLIER_KEY, // base64-encoded web key
    context = sdkContext,
    environment = Environment.PROD // or Environment.DEV
)`}</code></pre>
  );
}

<StartLibrary platform={props.platform}/>

:::tip Supplier key
MealzUIKit expects the **web** supplier key format (base64 JSON including `supplierId`, `origin`, `domain`, `noSupplier`, …) — the same key family used by Mealz SSR. If you do not already have a web/SSR key, Mealz will provide one for your integration.
:::

## User

`setUser` and `getCurrentUser` are **suspending** — call them from a coroutine (Kotlin) or an async context that can await Kotlin suspend functions (Swift). Call after `start`, and again whenever auth changes.

### Defaults

Out of the box (after `start`, no user calls):

- **Authless mode is on** — `isAuthlessModeEnabled()` is `true` until you call `disableAuthlessMode()`.
- **Identity is a guest** — `getCurrentUser()` is `User.Authless` with an id from `/v2/generate-authless-token` (fetched on first need). Guests can browse and fill a Mealz basket.

Call `disableAuthlessMode()` only if shoppers **must** log in before basket actions. While authless mode is off and the shopper is not logged in, adding to the Mealz basket (and similar actions) triggers your screen’s `redirectToLogIn` callback so you can open your login flow.

export function AuthlessModeApis(props) {
  if (props.platform === "ios") {
    return (
      <pre><code className="language-swift">{`SdkConfiguration.shared.isAuthlessModeEnabled() // true by default
SdkConfiguration.shared.enableAuthlessMode()
SdkConfiguration.shared.disableAuthlessMode()`}</code></pre>
    );
  }
  return (
    <pre><code className="language-kotlin">{`SdkConfiguration.isAuthlessModeEnabled() // true by default
SdkConfiguration.enableAuthlessMode()
SdkConfiguration.disableAuthlessMode()`}</code></pre>
  );
}

<AuthlessModeApis platform={props.platform}/>

export function NoSupplierAuthWarning(props) {
  return (
    <Admonition type="warning" title="No Supplier">
      <p>
        Nothing to change for user auth — keep the default guest behaviour. Do not call{' '}
        <code>setUser(&quot;…&quot;)</code>. See{' '}
        <Link to={`/docs/${props.platform}/features/no-supplier`}>No Supplier</Link>.
      </p>
    </Admonition>
  );
}

<NoSupplierAuthWarning platform={props.platform}/>

### Log in

When the shopper logs into **your** app:

export function LoginApis(props) {
  if (props.platform === "ios") {
    return (
      <pre><code className="language-swift">{`// suspend — call via your async / coroutine bridge
SdkConfiguration.shared.setUser(userId: "user-123")`}</code></pre>
    );
  }
  return (
    <pre><code className="language-kotlin">{`SdkConfiguration.setUser("user-123")`}</code></pre>
  );
}

<LoginApis platform={props.platform}/>

If the previous identity was `User.Authless` and authless mode is **on**, Mealz may **merge** the guest basket into the logged-in basket (only when a store is already selected; best-effort — failures do not block `setUser`). See [Login and logout](/docs/web_ssr/v2/set-up-and-usage/login-and-logout).

### Log out

On logout, fall back to a guest identity:

export function LogoutApis(props) {
  if (props.platform === "ios") {
    return (
      <pre><code className="language-swift">{`SdkConfiguration.shared.setUser(userId: nil)`}</code></pre>
    );
  }
  return (
    <pre><code className="language-kotlin">{`SdkConfiguration.setUser(null)`}</code></pre>
  );
}

<LogoutApis platform={props.platform}/>

- Leaving `User.Authenticated` switches to `User.Authless` with a **new** authless id from `/v2/generate-authless-token`.
- Already `User.Authless` reuses the stored id (no SSR call).
- Does **not** change the authless-mode flag. With mode still on (default), the shopper can fill a guest basket again; if you had disabled authless mode, basket actions keep calling `redirectToLogIn` until you re-enable it or `setUser("…")`.

### Session (optional)

Call **before** any SSR/WebView use if you need a fixed session id:

export function SessionApis(props) {
  if (props.platform === "ios") {
    return (
      <pre><code className="language-swift">{`SdkConfiguration.shared.forceSessionIdTo(uuid: "00000000-0000-0000-0000-000000000000")
let sessionId = SdkConfiguration.shared.getCurrentSessionId()`}</code></pre>
    );
  }
  return (
    <pre><code className="language-kotlin">{`SdkConfiguration.forceSessionIdTo("00000000-0000-0000-0000-000000000000")
val sessionId = SdkConfiguration.getCurrentSessionId()`}</code></pre>
  );
}

<SessionApis platform={props.platform}/>

## Store

export function StoreApis(props) {
  if (props.platform === "ios") {
    return (
      <>
        <p><strong>Retailer mode</strong> — set the shopper's store with your POS / external id:</p>
        <pre><code className="language-swift">{`SdkConfiguration.shared.setSelectedStoreWithExternalId(externalStoreId: "store-ext-42")`}</code></pre>
        <p>
        <strong>No Supplier mode</strong> — do <strong>not</strong> call <code>setUser("…")</code> and do <strong>not</strong> set a
        store in the normal flow. Leave authless defaults; store (and retailer) selection is handled by the{' '}
        <a href="/docs/web_ssr/v2/non-retailer-integration/introduction">Supplier Selector</a> flow:
        Mealz opens it automatically, or other No Supplier components request it when a store is required.
      </p>
      <pre><code className="language-swift">{`// Only for a specific advanced need — prefer letting SupplierSelector drive this
SdkConfiguration.shared.setSelectedStoreWithMealzId(mealzStoreId: "mealz-store-id")`}</code></pre>
      </>
    );
  }
  return (
    <>
      <p><strong>Retailer mode</strong> — set the shopper's store with your POS / external id:</p>
      <pre><code className="language-kotlin">{`SdkConfiguration.setSelectedStoreWithExternalId("store-ext-42")`}</code></pre>
      <p>
        <strong>No Supplier mode</strong> — do <strong>not</strong> call <code>setUser("…")</code> and do <strong>not</strong> set a
        store in the normal flow. Leave authless defaults; store (and retailer) selection is handled by the{' '}
        <a href="/docs/web_ssr/v2/non-retailer-integration/introduction">Supplier Selector</a> flow:
        Mealz opens it automatically, or other No Supplier components request it when a store is required.
      </p>
      <pre><code className="language-kotlin">{`// Only for a specific advanced need — prefer letting SupplierSelector drive this
SdkConfiguration.setSelectedStoreWithMealzId("mealz-store-id")`}</code></pre>
    </>
  );
}

<StoreApis platform={props.platform}/>

Using the wrong store API for the key mode throws (external ids are retailer-only; Mealz ids are No Supplier-only).

## First Integration Checklist

export function FirstIntegrationChecklist(props) {
  const basket = `/docs/${props.platform}/overview/basket`;
  const catalog = `/docs/${props.platform}/features/catalog`;
  const noSupplier = `/docs/${props.platform}/features/no-supplier`;
  const styling = `/docs/${props.platform}/advanced/styling`;
  const startCall =
    props.platform === "ios"
      ? "SdkConfiguration.shared.start(...)"
      : "SdkConfiguration.start(...)";

  return (
    <>
      <h3>Retailer vs No Supplier</h3>
      <p>
        You do <strong>not</strong> pick the mode in code. It is encoded in the supplier key Mealz gives you (
        <code>noSupplier</code> flag). Use the same <code>{startCall}</code> flow either way; APIs that only apply to one mode
        (retailer basket sync vs No Supplier helpers) simply become relevant depending on that key.
      </p>
      <p>
        For No Supplier UI helpers, see <Link to={noSupplier}>No Supplier</Link>.
      </p>

      <h3>Retailer Mode</h3>
      <ol>
        <li>Call <code>{startCall}</code> once at app launch.</li>
        <li>
          When the shopper logs in, call <code>setUser("…")</code>; on logout, call <code>setUser(null)</code> — both
          are <strong>suspend</strong>. See <Link to={`#user`}>User</Link> above.
        </li>
        <li>Set the store with <code>setSelectedStoreWithExternalId</code>.</li>
        <li>
          Register the basket synchronizer <strong>after</strong> <code>start</code> and <strong>before</strong> any Mealz screen — see{' '}
          <Link to={basket}>Basket Synchronization</Link>.
        </li>
        <li>
          Present a first screen (usually Catalog) — see <Link to={catalog}>Catalog</Link> under Features.
        </li>
        <li>
          Optional: register CSS manifests — <Link to={styling}>Styling</Link> (defaults already work).
        </li>
      </ol>

      <h3>No Supplier Mode</h3>
      <ol>
        <li>Call <code>{startCall}</code> with your No Supplier web key (mode is in the key).</li>
        <li>
          Do <strong>not</strong> identify the shopper (<code>setUser("…")</code>) and do <strong>not</strong> select
          a store — leave authless defaults and let{' '}
          <Link to={noSupplier}>Supplier Selector</Link> drive retailer/store picking.
        </li>
        <li>
          Present Catalog and/or No Supplier helpers (pricing, add-to-cart CTA) — see Features.
        </li>
        <li>
          Optional: register <code>BasketCountNotifier</code> listeners for recipe / product badges — see{' '}
          <Link to={noSupplier}>No Supplier</Link>.
        </li>
        <li>
          Optional: register CSS manifests — <Link to={styling}>Styling</Link>.
        </li>
      </ol>
    </>
  );
}

<FirstIntegrationChecklist platform={props.platform}/>

## Next Steps

export function InitNextSteps(props) {
  return (
    <ol>
      <li>
        <Link to={`/docs/${props.platform}/overview/basket`}>Basket Synchronization</Link> (retailer mode)
      </li>
      <li>Feature screens under <strong>Features</strong></li>
      <li>
        Optional branding: <Link to={`/docs/${props.platform}/advanced/styling`}>Styling</Link>
      </li>
    </ol>
  );
}

<InitNextSteps platform={props.platform}/>

## Optional Configuration

export function OptionalConfig(props) {
  const isIos = props.platform === "ios";
  const langCode = isIos ? (
    <pre><code className="language-swift">{`SdkConfiguration.shared.setSelectedLanguage(language: .fr) // .en, …
SdkConfiguration.shared.getCurrentLanguage()`}</code></pre>
  ) : (
    <pre><code className="language-kotlin">{`SdkConfiguration.setSelectedLanguage(Language.FR) // Language.EN, …
SdkConfiguration.getCurrentLanguage()`}</code></pre>
  );
  const profilingCode = isIos ? (
    <pre><code className="language-swift">{`SdkConfiguration.shared.enableProfiling()
SdkConfiguration.shared.disableProfiling()`}</code></pre>
  ) : (
    <pre><code className="language-kotlin">{`SdkConfiguration.enableProfiling()
SdkConfiguration.disableProfiling()`}</code></pre>
  );
  const labelsCode = isIos ? (
    <pre><code className="language-swift">{`SdkConfiguration.shared.enableCustomLabels()
SdkConfiguration.shared.disableCustomLabels()`}</code></pre>
  ) : (
    <pre><code className="language-kotlin">{`SdkConfiguration.enableCustomLabels()
SdkConfiguration.disableCustomLabels()`}</code></pre>
  );
  const debugCode = isIos ? (
    <pre><code className="language-swift">{`SdkConfiguration.shared.enableDebugMode()
SdkConfiguration.shared.disableDebugMode()`}</code></pre>
  ) : (
    <pre><code className="language-kotlin">{`SdkConfiguration.enableDebugMode()
SdkConfiguration.disableDebugMode()`}</code></pre>
  );

  let debugBlurb = "Developer-only. Do not ship with this enabled in production.";
  if (props.platform === "android") {
    debugBlurb =
      "Developer-only. Enables Chrome WebView debugging (`chrome://inspect`). Do not ship with this enabled in production.";
  } else if (props.platform === "ios") {
    debugBlurb =
      "Developer-only. Makes the Mealz WKWebView inspectable in Safari. Do not ship with this enabled in production.";
  }

  const iosWebInspectorSteps =
    props.platform === "ios" ? (
      <ol>
        <li>
          On the iPhone: <strong>Settings → Safari → Advanced → Web Inspector</strong> → On.
        </li>
        <li>
          Call <code>SdkConfiguration.shared.enableDebugMode()</code> after <code>start</code>.
        </li>
        <li>
          On a Mac with the device connected (or simulator): Safari → <strong>Develop</strong> → your device / simulator →
          pick the Mealz WKWebView page.
        </li>
      </ol>
    ) : null;

  return (
    <>
      <h3>Language</h3>
      <p><strong>Default:</strong> <code>Language.FR</code> (<code>fr</code>).</p>
      {langCode}
      <p>
        Sets the locale used for SSR content (<code>Language-id</code>). Values map to ISO language codes (
        <code>fr</code>, <code>en</code>, …). See{' '}
        <a href="/docs/web_ssr/v2/customization/internationalization">SSR internationalization</a> for custom label files.
      </p>

      <h3>Profiling</h3>
      <p><strong>Default:</strong> enabled.</p>
      {profilingCode}
      <p>
        Controls personalization across Mealz features (recommendations, tailored catalog content, …). Mirror the shopper's
        consent: if profiling / cookies are refused, call <code>disableProfiling()</code>. This maps to the SSR{' '}
        <code>profiling</code> header (<code>true</code> / <code>false</code>).
      </p>

      <h3>Custom Labels</h3>
      <p><strong>Default:</strong> disabled.</p>
      {labelsCode}
      <p>
        When enabled, SSR uses your <strong>custom i18n override</strong> files (if configured with Mealz) instead of the
        default Mealz copy. Keep this off unless you have custom label packs. Details:{' '}
        <a href="/docs/web_ssr/v2/customization/internationalization">SSR internationalization</a>.
      </p>

      <h3>Debug Mode</h3>
      <p><strong>Default:</strong> disabled.</p>
      {debugCode}
      <p>{debugBlurb}</p>
      {iosWebInspectorSteps}
    </>
  );
}

<OptionalConfig platform={props.platform}/>
