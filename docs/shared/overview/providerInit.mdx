export let platform 

import * as BasketService from '@site/docs/android/advanced/_shared/_miamManager/_basketService/';


import AndroidProviderId from '@site/docs/android/advanced/_shared/_miamManager/ProviderId.md';
import AndroidBasicImpProvider from '@site/docs/android/overview/_shared/_MealzBasicImpProvider.mdx'
import AndroidUserSetting from '@site/docs/android/overview/_shared/_MealzUserSetUp.mdx'
import AndroidStoreSetup from '@site/docs/android/overview/_shared/_MealzStoreSetupProvider.mdx'

import IosProviderId from '@site/docs/ios/advanced/_shared/_miamManager/ProviderId.md';
import IosBasicImpProvider from '@site/docs/ios/overview/_shared/_MealzBasicImpProvider.mdx'
import IosUserSetting from '@site/docs/ios/overview/_shared/_MealzUserSetUp.mdx'
import IosStoreSetup from '@site/docs/ios/overview/_shared/_MealzStoreSetupProvider.mdx'



export function ProviderId(props){
    if(props.platform == "android") { return <AndroidProviderId /> } else return  <IosProviderId />
}

export function BasicImpProvider(props){
    if(props.platform == "android") { return <AndroidBasicImpProvider /> } else return  <IosBasicImpProvider />
}

export function UserSetting(props){
    if(props.platform == "android") { return <AndroidUserSetting /> } else return  <IosUserSetting />
}

export function StoreSetup(props){
    if(props.platform == "android") { return <AndroidStoreSetup /> } else return  <IosStoreSetup />
}


You provide recipes and use Mealz to push it to a retailer. For example, a food delivery service that works with multiple grocery stores. 

## ProviderKey - base64 key to set information

<ProviderId platform= {props.platform}/>

:::tip
Your keys will be provided by our Development team
:::

## Basic implementation

There are several options to configure to handle authless users. The below is a basic implementation
where the Mealz initialization process will only start after the user is **logged in** and has **selected a valid store**.

First, to init Mealz we need your application context and your provider key

:::note
Two keys will have been sent to you: one for **development** and one for **production**.
:::

<BasicImpProvider platform ={props.platform}/>

## User setup

Here is how to pass the user ID to the SDK, directly within the host app:

:::info
You can also enable our **authless** feature, more information [**here**](../advanced/user-configuration/##AuthLess
).
:::

<UserSetting platform ={props.platform}/>

:::info
To get full list of user features check [**User configurations**](../advanced/user-configuration).
:::

## Store setup

For Mealz to work properly, your user must be connected to a specific store so we can accurately provide recipes with available ingredients.
To add the store that the user is currently at, you can use this code:

<StoreSetup platform ={props.platform}/>

:::info
You can also set a callback to redirect to your Store Selector, more information [**here**](../advanced/store-configuration/##SetStore
).
:::


Congratulations, **Mealz** is good to go 🥳