Consequently, this iOS SDK is organized in four main modules:

- `mealzcore`: Contains the core logic shared between iOS & Android
- `MealziOSSDK`: SDK that contains the core logic, Swift extensions from `mealzcore` & related Views