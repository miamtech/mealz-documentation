import BaseEmptyComponent from './BaseEmptyComponent.mdx';

### MealzInBasketButtonEmpty

To create your own empty Meals in Basket Button (default doesn't have a view), you create a class that implements `BaseEmptyComponent`.

<BaseEmptyComponent />